services:
  app.dev:
    env_file:
      - .env
    restart: always
    environment:
      BOT: droblox
      NODE_ENV: production
    build:
      context: .
      dockerfile: docker/app/Dockerfile.app.prod
